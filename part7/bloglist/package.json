{
    "scripts": {
        "backend_dev": "npm run dev --prefix backend",
        "frontend_dev": "npm run dev --prefix frontend",
        "dev": "concurrently \"npm run backend_dev\" \"npm run frontend_dev\"",
        "start": "npm run build --prefix frontend && npm run start --prefix backend",
        "delete_modules": "rm -rf backend/node_modules frontend/node_modules",
        "reinstall_dependencies": "npm run delete_modules && npm install --prefix backend && npm install --prefix frontend"
    },
    "dependencies": {
        "concurrently": "^9.2.4"
    }
}
